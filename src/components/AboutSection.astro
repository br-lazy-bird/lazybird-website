---

---

<section class="relative py-8 sm:py-12">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- About -->
    <div class="text-center mb-12">
      <p class="text-lg text-slate-600 max-w-3xl mx-auto">
        Technical interviews often test problems you may not have encountered in production.
        Lazy Bird bridges that gap. Each system is a realistic, broken application that lets you
        practice diagnosing and fixing real performance issues before you face them in interviews or on the job.
      </p>
    </div>

    <!-- How It Works -->
    <h3 class="text-xl font-semibold text-emerald-600 text-center mb-8">How It Works</h3>
    <div class="grid grid-cols-1 sm:grid-cols-3 gap-10 mb-10 max-w-4xl mx-auto">
      <div class="text-center">
        <div class="w-12 h-12 bg-emerald-600 text-white rounded-full flex items-center justify-center mx-auto mb-3 font-bold text-lg shadow-md">1</div>
        <h4 class="font-semibold text-slate-900 mb-1">Clone</h4>
        <p class="text-sm text-slate-600">Pick a broken system and run it locally with Docker</p>
      </div>
      <div class="text-center">
        <div class="w-12 h-12 bg-emerald-600 text-white rounded-full flex items-center justify-center mx-auto mb-3 font-bold text-lg shadow-md">2</div>
        <h4 class="font-semibold text-slate-900 mb-1">Debug</h4>
        <p class="text-sm text-slate-600">Investigate the issue using real tools and metrics</p>
      </div>
      <div class="text-center">
        <div class="w-12 h-12 bg-emerald-600 text-white rounded-full flex items-center justify-center mx-auto mb-3 font-bold text-lg shadow-md">3</div>
        <h4 class="font-semibold text-slate-900 mb-1">Fix</h4>
        <p class="text-sm text-slate-600">Implement the solution and verify it!</p>
      </div>
    </div>

    <!-- DETONADO mention -->
    <div class="bg-emerald-100 border border-emerald-300 rounded-lg p-4 text-center">
      <p class="text-slate-700">
        <span class="font-semibold text-emerald-700">Stuck?</span> Each system includes a DETONADO — a step-by-step guide with the solution if you need help.
      </p>
    </div>
  </div>
</section>

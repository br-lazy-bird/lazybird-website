---

---

<section class="relative py-12 sm:py-16">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Why Lazy Bird -->
    <div class="text-center mb-12">
      <h2 class="text-2xl font-bold text-slate-900 mb-4">Why Lazy Bird?</h2>
      <p class="text-lg text-slate-600 max-w-2xl mx-auto">
        Technical interviews often test problems you may not have encountered in production.
        Lazy Bird bridges that gap. Each system is a realistic, broken application that lets you
        practice diagnosing and fixing real performance issues before you face them in interviews or on the job.
      </p>
    </div>

    <!-- How It Works -->
    <h3 class="text-xl font-semibold text-slate-900 text-center mb-8">How It Works</h3>
    <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 mb-10">
      <div class="text-center">
        <div class="w-10 h-10 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center mx-auto mb-3 font-bold">1</div>
        <h4 class="font-semibold text-slate-900 mb-1">Clone</h4>
        <p class="text-sm text-slate-600">Pick a broken system and run it locally with Docker</p>
      </div>
      <div class="text-center">
        <div class="w-10 h-10 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center mx-auto mb-3 font-bold">2</div>
        <h4 class="font-semibold text-slate-900 mb-1">Debug</h4>
        <p class="text-sm text-slate-600">Investigate the issue using real tools and metrics</p>
      </div>
      <div class="text-center">
        <div class="w-10 h-10 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center mx-auto mb-3 font-bold">3</div>
        <h4 class="font-semibold text-slate-900 mb-1">Fix</h4>
        <p class="text-sm text-slate-600">Implement the solution and verify with tests</p>
      </div>
    </div>

    <!-- DETONADO mention -->
    <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 text-center">
      <p class="text-slate-700">
        <span class="font-medium">Stuck?</span> Each system includes a DETONADO — a step-by-step guide with the diagnosis process and solution if you need help.
      </p>
    </div>
  </div>
</section>

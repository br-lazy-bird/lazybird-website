---

---

<section class="relative py-12 sm:py-16">
  <div class="relative max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <img
      src="/lazy-bird.jpeg"
      alt="Lazy Bird mascot"
      class="w-32 h-32 sm:w-40 sm:h-40 mx-auto mb-6 rounded-full shadow-lg ring-4 ring-white"
    />

    <h1 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-slate-900 mb-4">
      Lazy Bird catches the bug
    </h1>

    <h4 class="text-base sm:text-lg font-normal text-slate-500 mb-1">
      but is too lazy to fix it.
    </h4>
    <!-- Accent line -->
    <li></li>
    <div class="w-24 h-1 bg-gradient-to-r from-blue-500 to-blue-600 mx-auto mb-8 rounded-full"></div>

    <!-- Brief explanation -->
    <p class="text-lg text-slate-600 max-w-2xl mx-auto mb-8">
      A hands-on learning platform with intentionally broken systems.
      Clone, run, debug, and fix real-world performance problems.
    </p>

    <!-- Feature highlights -->
    <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 max-w-3xl mx-auto mb-10">
      <div class="bg-white/70 backdrop-blur-sm border border-slate-200 rounded-lg p-4 shadow-sm">
        <div class="text-blue-600 font-semibold mb-1">Self-Contained</div>
        <div class="text-sm text-slate-600">Docker-first environments ready to run</div>
      </div>
      <div class="bg-white/70 backdrop-blur-sm border border-slate-200 rounded-lg p-4 shadow-sm">
        <div class="text-blue-600 font-semibold mb-1">Real Problems</div>
        <div class="text-sm text-slate-600">Database, concurrency, event-driven issues</div>
      </div>
      <div class="bg-white/70 backdrop-blur-sm border border-slate-200 rounded-lg p-4 shadow-sm">
        <div class="text-blue-600 font-semibold mb-1">Learn by Fixing</div>
        <div class="text-sm text-slate-600">Diagnose, understand, and solve</div>
      </div>
    </div>

    <div class="flex justify-center gap-4">
      <a
        href="/systems"
        class="bg-blue-600 text-white px-8 py-4 rounded-lg font-medium text-lg hover:bg-blue-700 transition-colors shadow-md hover:shadow-lg"
      >
        Get Started
      </a>
    </div>
  </div>
</section>

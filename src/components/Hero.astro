---

---

<section class="relative pt-12 sm:pt-16 pb-5">
  <div class="relative max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <img
      src="/lazy-bird.jpeg"
      alt="Lazy Bird mascot"
      class="w-32 h-32 sm:w-40 sm:h-40 mx-auto mb-6 rounded-full shadow-lg ring-4 ring-emerald-100"
    />

    <h1 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-slate-900 mb-2">
      Lazy Bird catches the bug
    </h1>

    <p class="text-lg sm:text-xl text-slate-500 mb-4">
      but is too lazy to fix it.
    </p>

    <div class="w-16 h-1 bg-gradient-to-r from-emerald-400 to-emerald-600 mx-auto rounded-full"></div>
  </div>
</section>

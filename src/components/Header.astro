---
const navLinks = [
  { href: '/systems', label: 'Systems' },
  { href: '/blog', label: 'Blog' },
  { href: 'https://github.com/br-lazy-bird', label: 'GitHub', external: true },
];
---

<header class="bg-white border-b border-slate-200">
  <nav class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <a href="/" class="flex items-center gap-2 text-xl font-bold text-slate-900 hover:text-blue-600 transition-colors">
        <span>Lazy Bird</span>
      </a>

      <div class="flex items-center gap-6">
        {navLinks.map((link) => (
          <a
            href={link.href}
            class="text-slate-600 hover:text-blue-600 font-medium transition-colors"
            target={link.external ? '_blank' : undefined}
            rel={link.external ? 'noopener noreferrer' : undefined}
          >
            {link.label}
          </a>
        ))}
      </div>
    </div>
  </nav>
</header>
